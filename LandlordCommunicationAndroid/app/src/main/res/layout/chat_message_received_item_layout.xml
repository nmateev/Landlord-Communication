<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentEnd="true"
    android:layout_marginTop="2dp"
    android:layout_marginEnd="2dp">

    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/civ_receiver_image"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="5dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="2dp"
        android:layout_marginBottom="2dp"
        app:civ_border_color="#641D79"
        app:civ_border_width="2dp" />


    <TextView
        android:id="@+id/tv_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="2dp"
        android:layout_marginStart="2dp"
        android:layout_toStartOf="@+id/civ_receiver_image"
        android:background="@drawable/rounded_chat_bubble_accent"
        android:maxWidth="220dp"
        android:padding="8dp"
        android:textColor="@color/colorWhite" />

    <TextView
        android:id="@+id/text_message_time_sent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/tv_message"
        android:layout_toStartOf="@+id/tv_message"
        android:textColor="@color/colorAccent"
        android:textSize="11sp" />
</RelativeLayout>